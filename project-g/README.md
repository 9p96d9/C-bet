# プロジェクトG 工程表ツール ― ステップ 1（往路）

共通仕様（`00_共通仕様…`）と 設計B（`02_設計B_HTMLツール_往路→復路.md`）を
最上位仕様とする実装。**ステップ 2（復路）には着手していない。**

> **呼び名について**：本書では工程管理 SaaS を **プロジェクトG** と書く。上位仕様の文書では製品名で書かれているものと同じものを指す。

## 納品物

| ファイル | 内容 |
|---|---|
| `プロジェクトG_工程表ツール.html` | 単一 HTML。CSS・JS・ExcelJS 4.4.0 を全てインライン。`file://` で動く |
| `設計B_実装メモ.md` | 描画規則表（PDF 実測）・推定の記録・検査ログ・自前コード全文 |
| `out/サポートルーム_サンプル工程表_20260901-20261010.xlsx` | サンプルから生成した xlsx |
| `docs/ファイル仕様書_チャットAI用.md` | どのファイルが何を出していて誰が使うか（自動生成） |
| `docs/ファイル仕様書_人間用.md` | なぜそうなっているか・直すときに何を気にするか |
| `tools/開発用.html` | `src/` を直接読む開発用ページ。組み立て不要 |
| `tools/組み立て.html` | ブラウザだけで「1 枚に組み立てる／1 枚をばらす」 |

**`設計B_実装メモ.md` の 0 章を最初に読むこと。**

## 使い方

`プロジェクトG_工程表ツール.html` をブラウザで開き、CSV を選んで［描画］。
SVG 検査が全件 OK になると［xlsx 書き出し］が有効になる。

**CSV に値が無く、ツールが規則で埋めた箇所は読み込みのたびに一覧で出る。**
「推定」と出た行は CSV から決められない値なので、プロジェクトG の画面で確認してほしい。
gate の中間行は「gate 中間行の指定」に `<工程ID>:<行>` を入れれば上書きできる。

## 工程表の絵はどこから来ているか

**CSV の値だけから、その場で描いています。** 渡した PDF を貼っているのではありません。

- HTML の中に画像は 1 枚も入っていない（`<image>`・`data:image`・PNG/JPEG・PDF のいずれも 0 件）。
  内訳は ExcelJS が 949KB、自前コードが 79KB、HTML と CSS が 5KB。
- 描画は `src/03-render.js` が `document.createElementNS` で
  `<path>` `<polygon>` `<rect>` `<circle>` `<text>` を組み立てている。
  座標はすべて CSV の日付・行番号から `x = 日付index × DAY_W`、
  `y = (行−1) × ROW_H + ROW_H/2` で計算する。
- PDF は **開発時に描画規則を実測するため**と、
  **`tools/compare-pdf.py` が答え合わせに使うため**にだけ登場する。
  ツール本体（HTML）は PDF を読まないし、同梱もしていない。

自分で確かめるには、CSV の値を変えて読み込み直せばよい。
例：A1 の `開始日`/`終了日` を 10 日ずらし、`工程線の色` を緑、`工程線の太さ` を 6 にすると、

| | 元の CSV | 書き換えた CSV |
|---|---|---|
| A1 の日付 | 2026-09-07〜09-14 | 2026-09-17〜09-24 |
| 最初の区間の `d` | `M 144 210 …` | `M 384 210 …`（10 日 × 24px = 240px 右） |
| 線色・太さ | `#ec635e` / 1.5 | `#00a000` / 6 |

結果は `out/demo_CSVを書き換えた描画_full.png`。A1 が緑の太い矢印になって右へ動き、
つながっていた A2 のノードから離れているのが分かる。
表示期間や 1 日の幅を変えても、SVG の幅と座標がそのつど計算し直される
（例：9/24–10/5・1 日 48px なら幅 576px、描画 11 件）。

## 別の工程表の CSV でも動くか

**同じ製品が出した CSV なら動きます。** 実際に変種を作って通しました
（`node tools/robustness-test.mjs`、14 件すべて期待どおり）。

| 変えたところ | 結果 |
|---|---|
| 列順を逆にした | 動く（列は見出し名で引いているため） |
| 使わない列を 52 個削った（124 → 72 列） | 動く |
| 知らない列を 5 個足した（124 → 129 列） | 動く |
| 必須列（`工程線の形状`）が無い | **読み込みで止まり、欠けている列名を出す** |
| BOM 無し・改行が LF | 動く |
| 別の年（2028 年） | 動く |
| 工程 230 件・行 460 | 動く（569ms、SVG 検査 3237 項目も全件 OK） |
| 工程 1 件／0 件 | 動く |
| `0.5日` に値がある | 動く（**警告**を出し、日付だけで描く） |
| `工程削除` に値がある | 動く（その工程を描かない） |
| `textSize = S`（サンプルに無い） | 動く（**推定として記録**される） |
| `crank` に中間ノードがある（サンプルに無い） | 動く |
| 知らない形状名 | 動く（直線として描き、警告を出す） |
| 関係線が 3 本 | 動く |
| **`休日` 列が自前の計算と食い違う** | **検査が拾って xlsx 書き出しを止める** |

### 別の工程表で注意がいるところ

1. **会社休日・祝日以外の休みがある工程表**
   本ツールは休日を「土日＋日本の祝日」で計算し、CSV の `休日` 列で検算する。
   食い違うと検査が NG になり、**xlsx 書き出しボタンが無効のまま**になる。
   黙って違う絵を出すよりは止める方を選んだ。その工程表を扱うには
   休日の規則を足す必要がある（`src/00-holiday.js` の 1 箇所）。

2. **`gate` の中間ノードの行**
   CSV にこの行番号が無いのはサンプル固有の話ではないので、
   **gate が多い工程表ほど推定が増える**。サンプルは 5 本中 2 本だったが、
   230 件に増やした試験では 20 本が推定になった。
   推定は全部一覧に出るので、画面の「gate 中間行の指定」で潰せる。

3. **`textSize = S` の実寸**
   サンプルに 1 件も無いため測れていない。使われていたら推定として記録される。

## 画面の見かた

- **状態バー**（図のすぐ下）に、工程数・描画数・検査の結果・警告・
  **要確認の推定の件数**が常に出る。ログは下へ流れて埋もれるので、
  ここだけは常に見えるようにしてある。
- **［推定を表示］**を押している間だけ、CSV に値が無くツールが規則で
  埋めた箇所が図の上に出る。押していないときは普通の工程表のまま。
  - 規則で推定した工程 … オレンジの丸と、推定した行に重ねた帯＋ラベル
  - それ以外の工程 … 薄くなる
  - PDF 実測の既定値（太さ・色が空だった工程）… 小さな灰色の点
  - 押すと最初の推定まで自動で送る。下の一覧の行をクリックすると個別に飛べる
- **工程にマウスを当てる**と、名前・形状・日付・日数・行・
  CSV に無く埋めた項目が出る。
- 検査結果は **既定では 1 行の要約だけ**。「OK の N 件を見る」で開く。
  開いたときに初めて表を組むので、工程が多くても画面が重くならない
  （工程 506 件でログの DOM は 334 要素。畳まない実装だと 39,305 要素になっていた）。

## うまくいかないときの診断ログ

現場の工程表で試して思ったとおりにならなかったら、
画面右上の **［診断ログを書き出す］** を押してください。
`診断ログ_YYYYMMDD_HHMM.txt` が保存されます。これを送ってもらえれば
こちらで原因を追えます。

**工程表の中身は入りません。**

| 入れないもの | どうなるか |
|---|---|
| プロジェクトID・工程表ID・ユーザーID・ユーザー名 | まるごと除外 |
| 工程ID・項目ID | `P001` `N001` のような通し番号に置換（対応表は出さない） |
| 工程線名・項目名 | `(3文字)` のように文字数だけ |
| ファイル名 | 拡張子だけ（`(ファイル名).csv`） |
| 協力会社・詳細工程・タグ | まるごと除外 |

| 入れるもの | なぜ要るか |
|---|---|
| 形状・行番号・日付・色・太さ・矢印・点線・斜行 | 描画がずれた原因を追うため |
| 見出しの列名 | 別の工程表で列の構成が違うかを見るため |
| 件数・分布・SVG の大きさ | 規模と傾向を見るため |
| 検査の NG 項目（期待値と実測値の数値） | どこがどう違ったかを特定するため |
| 警告・推定の一覧 | CSV に無くて埋めた箇所を知るため |
| JS エラーとスタック | 落ちた場所を特定するため |
| ブラウザの種類・画面の大きさ | 環境依存かを切り分けるため |
| 表示期間内の祝日と `休日` 列との食い違い | 休日の規則が合っているかを見るため |

**書き出す前に全文が画面に出ます。** 何が出ていくか隠していないので、
送る前に目で確かめられます。例は `out/診断ログの例.txt`。

読み込みが途中で失敗したときも書き出せます。
その場合は CSV の素性（行数・列数・BOM・改行・引用符の数）と
エラーの中身が入るので、それだけで原因が分かることが多いはずです。

`実値のまま` にチェックを入れると伏せ字をやめられますが、
**社外に出さないでください**（画面にも赤字で出ます）。

検査：`node tools/diag-test.mjs`（23 項目）。
本物のサンプルから機微な文字列を 59 種類拾い、
伏せ字ログにそれが 1 つも含まれないことを毎回確かめています。

## 検査

| 検査 | 実行 | 結果 |
|---|---|---|
| PDF との座標照合（受け入れ 1） | `python3 tools/compare-pdf.py` | 26 / 28（共通規則で埋めた場合） |
| 同上・gate 中間行を手入力 | `python3 tools/compare-pdf.py --manual` | **28 / 28 全一致** |
| 受け入れ 2〜5 ＋ 回帰 | `node tools/acceptance.mjs` | ALL PASS |
| 祝日計算 | `node tools/holiday-test.mjs` | 16 / 16 |
| xlsx の独立検査（openpyxl） | `python3 tools/verify-xlsx-independent.py` | 21 / 21 |
| 診断ログに中身が漏れていないか | `node tools/diag-test.mjs` | ALL PASS |
| 壊れた CSV・大きい CSV | `node tools/robustness-test.mjs` | ALL PASS |
| 開発用.html と 1 枚が同じ結果か | `node tools/dev-check.mjs` | ALL PASS（SVG が 1 文字も違わない） |
| 組み立て.html が Node と同じ HTML を作るか | `node tools/assemble-check.mjs` | ALL PASS（1,065,284 文字が完全一致） |
| 仕様書に嘘のファイル名・関数名が無いか | `node tools/docs-check.mjs` | ALL PASS |

### 速さ（実測）

| 工程数 | 読込 | 描画 | 検査 | xlsx | 合計 | SVG 要素 | ログ DOM |
|---|---|---|---|---|---|---|---|
| 23 | 27ms | 6ms | 3ms | 117ms | 153ms | 452 | 40 |
| 230 | 22ms | 18ms | 16ms | 247ms | 303ms | 3,791 | 166 |
| 506 | 33ms | 40ms | 55ms | 460ms | 588ms | 8,243 | 334 |

## 開発

`プロジェクトG_工程表ツール.html` は生成物。**直さずに `src/` を直して作り直す。**

### Node が無い環境（現場の PC）

1. `tools/組み立て.html` を開いて、1 枚の HTML を**分解**する（`src.zip` が落ちてくる）
2. `src/` の中の js を 1 個だけチャット AI に渡して直す
   （ファイル先頭の「自動生成」の枠が、前後関係の説明になっている）
3. `tools/開発用.html` を開いて CSV を読ませる。ログに NG が無ければ OK
4. `tools/組み立て.html` で**組み立て**て 1 枚に戻す
5. 戻した 1 枚をブラウザで開いて、もう一度確かめる

詳しくは `docs/ファイル仕様書_人間用.md` の 2 章。

### Node がある環境

作り直しと検査を全部まとめて回すなら：

```
sh tools/all.sh
```

個別に回すなら：

```
node tools/build-html.mjs                 # src/ → 1 枚の HTML と tools/開発用.html
node tools/analyze-src.mjs                # 依存と公開名を数える（out/src-map.json）
node tools/gen-headers.mjs                # src/*.js 先頭の枠を書き直す（--check で確認のみ）
node tools/build-spec.mjs                 # ファイル仕様書_チャットAI用.md を作り直す
node tools/build-memo.mjs                 # docs/memo-body.md → 設計B_実装メモ.md
node tools/make-fixture.mjs               # 合成 CSV（禁止事項 1 の確認用）
python3 tools/pdf-extract.py              # PDF からベクター座標を抜く
python3 tools/make-package.py             # 配布 zip（Windows で文字化けしない形式）
```

**コードを直したら `gen-headers` と `build-spec` を必ず回す。**
ヘッダーと仕様書はコードから自動で作っているので、回さないとズレたまま残る。
`node tools/gen-headers.mjs --check` は書き換えずにズレだけを報告する。
